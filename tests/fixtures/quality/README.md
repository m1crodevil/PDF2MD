# Quality evaluator smoke fixture

This tiny deterministic fixture is intentionally committed to exercise the evaluator
without requiring OCR models, an API key, or a specific PDF. It covers plain text,
numeric retention, Markdown structure, and a table-detected page. Real PDF corpus
fixtures should be added separately with licensing/provenance and stable hashes.
